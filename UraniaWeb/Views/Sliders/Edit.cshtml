@model UraniaWeb.Models.Slider

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Slider</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdSlider" />
            <div class="form-group">
                <label asp-for="TitleSlider" class="control-label"></label>
                <input asp-for="TitleSlider" class="form-control" />
                <span asp-validation-for="TitleSlider" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UrlSlider1" class="control-label"></label>
                <input asp-for="UrlSlider1" class="form-control" />
                <span asp-validation-for="UrlSlider1" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
