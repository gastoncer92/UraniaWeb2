@model IEnumerable<UraniaWeb.Models.Slider>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TitleSlider)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UrlSlider1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TitleSlider)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UrlSlider1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSlider">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSlider">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSlider">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
